/**
 * Use full name for const and arg for the convenience of conversion.
 */
public enum VirtualSegment {
    CONSTANT,
    ARGUMENT,
    LOCAL,
    STATIC,
    THIS,
    THAT,
    POINTER,
    TEMP;
}
